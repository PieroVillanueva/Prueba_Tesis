using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonMetodos : MonoBehaviour
{
    // Start is called before the first frame update
    public SphereMetodoVR esfera;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void cambiarColorEsferaBoton()
    {
        esfera.cambiarColor();
    }
}
